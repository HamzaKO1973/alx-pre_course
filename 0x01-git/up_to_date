# Command to ensure you are on the main branch
git checkout master

# Command to fetch the latest changes from the remote main branch
git fetch origin master

# Command to merge the changes from the remote main branch into your local main branch
git merge origin/master

